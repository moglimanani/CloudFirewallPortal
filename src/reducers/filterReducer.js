// import * as types from '../actions';

const initialState = {
  filters: {}
};
export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
